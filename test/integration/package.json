{
  "name": "integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for Rebar Blockchain",
  "scripts": {
    "test": "mocha *.integration.test.js --recursive --exit"
  },
  "dependencies": {
  "fabric-network": "^2.2.20",
  "fabric-ca-client": "^2.2.20"
  },

  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.7",
    "fabric-network": "^2.2.20"
  }
}
